const decrementType = 'COUNTER_DECREMENT';
const resetType = 'COUNTER_RESET';
const initializeType = 'COUNTER_INIT';

const types = {
  incrementType: 'COUNTER_INCREMENT',
  decrementType,
  resetType,
  initializeType,
};

export default types;
