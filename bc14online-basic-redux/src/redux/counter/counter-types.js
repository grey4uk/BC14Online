const incrementType = 'INCREMENT';
const decrementType = 'DECREMENT';
const resetType = 'RESET';
const initializeType = 'INIT';

const types = {
  incrementType,
  decrementType,
  resetType,
  initializeType,
};

export default types;
